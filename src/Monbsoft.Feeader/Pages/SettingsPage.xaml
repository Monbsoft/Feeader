<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Monbsoft.Feeader.Controls"
             xmlns:res="clr-namespace:Monbsoft.Feeader.Resources.Strings"
             
             x:Class="Monbsoft.Feeader.Pages.SettingsPage"
             Title="{x:Static res:StringResources.Settings}"
             BackgroundColor="White">
    <Grid Padding="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Text="Feeds" />
        <controls:EditFeedControl FeedUrl="{Binding NewFeedUrl}"
                                  AddCommand="{Binding AddFeedCommand}"
                                  Grid.Column="1" />
    </Grid>
</ContentPage>